#include <stdio.h>
#include <stdlib.h>
#include "Funciones.h"
#define A 3
#define VACIO 0
#define OCUPADO 1
#define ELIMINADO -1

int main()
{
    eAlumno listaAlumnos[A];

    inicializarAlimnos(listaAlumnos,)

    cargarListado(listaAlumnos, A);
    mostrarListado(listaAlumnos, A);

    return 0;
}







